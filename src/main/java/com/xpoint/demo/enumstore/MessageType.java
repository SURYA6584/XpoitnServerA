package com.xpoint.demo.enumstore;

public enum MessageType {
    TEXT,
    FILE,  
    IMAGE,  
    DOCUMENT, 
    VIDEO,
    
    // Specific document types under the DOCUMENT category
    PDF,
    DOC,
    DOCX,
    XLS,
    XLSX,
    PPT,
    PPTX,ZIP
}
